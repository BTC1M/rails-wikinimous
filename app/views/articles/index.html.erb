
<% @articles.each do |article| %>
  <ul>
    <h3 class="title"> <%= link_to article.title, article_path(article.id) %> </h3>
    <%= article.content %>
  </ul>
<% end %>

<button type="button" class="btn btn-dark"><%= link_to "Create a new article", new_article_path %></button>
